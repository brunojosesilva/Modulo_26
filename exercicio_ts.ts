function ResultadoAB (numeroA: number, numeroB: number): number {
    return numeroA * numeroB;
}

console.log(ResultadoAB(10, 20));

function Saudacoes (nome: string): string {
    return `Ola ${nome}`;
}

console.log(Saudacoes);